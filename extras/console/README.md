# extras/console

A legacy web console over the same replay files. Not part of the shipped
surface; kept because it is working, tested code. The product is the
`strikeone` CLI and the TUI.

    uv run python extras/console/console.py --data data/processed/holdout_replay.parquet
